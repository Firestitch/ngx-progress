import { InjectionToken } from '@angular/core';


export const FS_PROGRESS_DEFAULT_CONFIG = new InjectionToken<any[]>('fs-progress.default-config');
export const FS_PROGRESS_CONFIG = new InjectionToken<any[]>('fs-progress.config');
